export default function Loading() {
    return (
    <div className="w-full h-screen bg-purple-400 flex items-center justify-center">
        <p>Page is loading... Hopefully not for too long :)</p>
    </div>)
}